#include "图形.h"
#include "常量.h"
void C图形::f初始化(HWND a窗口) {
	m三维.f初始化(a窗口);
	m二维.f初始化(m三维);
	m画图形 = m二维.fc画图形();
}
void C图形::f销毁() {
	m三维.f销毁();
}
void C图形::f计算() {

}
void C图形::f更新() {

}
void C图形::f显示() {
	auto &v渲染控制 = m三维.fg渲染控制();
	v渲染控制.f清屏();
	//m画图形->f填充矩形(数学::S矩形::fc坐标半尺寸(数学::S向量2::c零, 数学::S向量2::fc相同(100)));
	m画图形->f填充圆形(数学::S圆形::fc坐标半径(数学::S向量2::c零, 100));
	v渲染控制.f显示();
}